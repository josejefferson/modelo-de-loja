<script>
	(async () => {
		// <% locals.successMsg?.forEach(msg => { %>
		await toast(<%-JSON.stringify({ msg })%>.msg)
		// <% }) %>

		// <% locals.errorMsg?.forEach(msg => { %>
		await toast(<%-JSON.stringify({ msg })%>.msg, 'error')
		// <% }) %>

		// <% locals.warningMsg?.forEach(msg => { %>
		await toast(<%-JSON.stringify({ msg })%>.msg, 'warning')
		// <% }) %>

		// <% locals.infoMsg?.forEach(msg => { %>
		await toast(<%-JSON.stringify({ msg })%>.msg, 'info')
		// <% }) %>
	})()
</script>